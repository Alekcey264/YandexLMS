Теперь нужно настроить отображение работ в удобном виде, то есть в виде таблицы со стандартными полями. В качестве заголовка каждой работы выводится ее id, далее следует таблица, в которой последовательно записаны:

Название работы, фамилия и имя ответственного, продолжительность в часах, список id команды, завершена или нет.

При отображении используется расширение базового шаблона миссии.

Ссылку на архив с файлами и папками на github`е отправьте на проверку.

Примечания
Задание проверяется преподавателем.