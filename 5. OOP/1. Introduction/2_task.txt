Напишите класс Avalanche, реализующий метод go().
При первом вызове метода печатается буква O латинская в верхнем регистре. При каждом следующем вызове все O из предыдущей строки заменяются на ooOoo, а o на oOo.